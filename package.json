{
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.11",
    "@sentry/node": "10.69.0",
    "fflate": "0.8.3"
  },
  "overrides": {
    "tmp": "^0.2.6"
  },
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "main": "netlify/functions/lib/shared.js",
  "scripts": {
    "test": "node --test --test-concurrency=1 netlify/tests/**/*.test.mjs mcp/tests/*.test.mjs",
    "//test": "--test-concurrency=1 is required, not tuning: both suites shell out to `dart run`, which shares .dart_tool state, and concurrent files intermittently fail one of the Dart invocations.",
    "og:image": "node tools/generate-og-image.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@resvg/resvg-js": "2.6.2",
    "netlify-cli": "27.0.1"
  }
}
