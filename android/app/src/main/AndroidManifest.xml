<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
    Required for url_launcher to open external URLs and interact with internet resources.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--
    Required for managing the ScheduledNotificationBootReceiver,
    particularly useful for boot-time-related states.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!--
    Enables vibration for notifications enabled by NotificationService._enableVibration.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
    Necessary for full-screen notification intents with NotificationService.fullScreenIntent.
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Add these when flutterLocalNotificationsPlugin.zonedSchedule()
         is used to schedule notification on exact date and time.
         Example use could be to schedule a notification like
         ```Dart
            await flutterLocalNotificationsPlugin.zonedSchedule(
              42, // unique notification ID
              'Upcoming Drill',
              'Your RingDrill starts in 10 minutes',
              scheduledTime, // a TZDateTime 10 minutes before start
              const NotificationDetails(
                android: AndroidNotificationDetails(
                  'exercise_reminders',
                  'Drill Reminders',
                  channelDescription: 'Notifications before drills begin',
                  importance: Importance.high,
                  priority: Priority.high,
                ),
              ),
              androidAllowWhileIdle: true, // needed for Doze
              uiLocalNotificationDateInterpretation:
                  UILocalNotificationDateInterpretation.absoluteTime,
              matchDateTimeComponents: DateTimeComponents.dateAndTime,
            );
         ```
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    !-->
    <!--
    Essential for posting local notifications with FlutterLocalNotificationsPlugin.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!--
    Required for managing notifications and other key foreground services via FlutterLocalNotificationsPlugin.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!--
    Used in conjunction with android.permission.FOREGROUND_SERVICE to justify its special use for app publishing.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!--
    Needed for channelBypassDnd: true, allowing notifications to bypass Do Not Disturb mode.
    -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY"/>

    <application
        android:label="RingDrill"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
        >
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <service
            android:name="com.dexterous.flutterlocalnotifications.ForegroundService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="The foreground service ensures that time-sensitive alarms and critical user notifications (such as emergency alert tones or timed drills) are reliably triggered when the app is running exercises even when the device is idle or locked."
            />
        </service>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- If your application checks for inAppBrowserView launch mode support -->
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
